'use strict'

var should = require("chai").should();

describe("Universe", function () {
    it("should be self-consistent", function () {
        // expect(2).to.equal(2);
        (2).should.equal(2);
    });
});
